# تیکی ایران (Tikkie Iran) - نسخه دمو

<div dir="rtl">

سیستم پرداخت همتا به همتا (P2P) برای ایران، شبیه به Tikkie و Bizum

**🎭 این نسخه دمو/نمایشی کاملا عملیاتی است و بدون نیاز به API خارجی، پایگاه داده، یا سرویس‌های واقعی کار می‌کند.**

</div>

---

## 🌟 ویژگی‌ها

<div dir="rtl">

### Backend (Node.js + Express)
- ✅ سرویس موک شتاب (Shetab) با اعتبارسنجی کارت‌های ایرانی
- ✅ سرویس موک پیامک با نمایش در کنسول
- ✅ پایگاه داده حافظه‌ای (In-Memory) با قابلیت ذخیره در JSON
- ✅ احراز هویت با JWT
- ✅ داده‌های دمو واقع‌گرایانه با نام‌ها و توضیحات فارسی
- ✅ تمامی endpoint های API کاملا عملیاتی

### Mobile (React Native + Expo)
- ✅ رابط کاربری فارسی کامل
- ✅ بنر دمو در تمام صفحات
- ✅ سه کاربر دمو از پیش تعریف شده
- ✅ دکمه "ورود سریع" برای حساب‌های دمو
- ✅ ذخیره‌سازی آفلاین با AsyncStorage
- ✅ انیمیشن‌ها و UI مدرن و حرفه‌ای

</div>

---

## 🚀 راه‌اندازی سریع (بدون نیاز به پیکربندی)

### پیش‌نیازها
```bash
# Node.js 16+ و npm
node --version
npm --version
```

### 1️⃣ نصب و اجرای Backend

```bash
cd backend
npm install
npm run dev
```

✅ Backend در آدرس `http://localhost:3000` اجرا می‌شود

### 2️⃣ نصب و اجرای Mobile App

```bash
cd mobile
npm install
npm start
```

✅ Expo DevTools باز می‌شود. می‌توانید:
- روی `w` بزنید تا در مرورگر باز شود
- روی `i` بزنید برای iOS Simulator
- روی `a` بزنید برای Android Emulator
- اپ Expo Go را روی گوشی نصب کنید و QR Code را اسکن کنید

---

## 👤 حساب‌های دمو

<div dir="rtl">

| شماره موبایل | رمز عبور | نام |
|--------------|----------|-----|
| 09123456789 | demo1234 | علی احمدی |
| 09121111111 | demo1234 | سارا محمدی |
| 09122222222 | demo1234 | رضا کریمی |

</div>

---

## 📁 ساختار پروژه

```
tikkie-iran/
├── backend/
│   ├── src/
│   │   ├── controllers/
│   │   │   ├── auth.controller.js       # ثبت نام و ورود
│   │   │   ├── user.controller.js       # پروفایل و کارت‌ها
│   │   │   └── payment.controller.js    # پرداخت و درخواست‌ها
│   │   ├── services/
│   │   │   ├── shetab.service.js        # موک شتاب
│   │   │   └── sms.service.js           # موک پیامک
│   │   ├── middleware/
│   │   │   ├── auth.js                  # احراز هویت JWT
│   │   │   └── validation.js            # اعتبارسنجی
│   │   ├── database/
│   │   │   ├── connection.js            # پایگاه داده حافظه‌ای
│   │   │   └── mockData.js              # تولید داده دمو
│   │   └── server.js                    # سرور اصلی
│   ├── data/
│   │   └── demo-data.json               # داده‌های دمو (خودکار ایجاد می‌شود)
│   ├── package.json
│   └── .env                             # تنظیمات (DEMO_MODE=true)
│
├── mobile/
│   ├── src/
│   │   ├── screens/
│   │   │   ├── LoginScreen.js           # صفحه ورود
│   │   │   ├── RegisterScreen.js        # صفحه ثبت نام
│   │   │   ├── HomeScreen.js            # صفحه اصلی
│   │   │   ├── CardsScreen.js           # مدیریت کارت‌ها
│   │   │   └── CreateRequestScreen.js   # ایجاد درخواست پرداخت
│   │   └── services/
│   │       └── api.js                   # سرویس API با حالت دمو
│   ├── App.js                           # اپلیکیشن اصلی
│   ├── package.json
│   └── app.json
│
└── README.md
```

---

## 🎯 قابلیت‌های دمو

<div dir="rtl">

### ✅ احراز هویت
- ثبت نام با کد ملی ایرانی (اعتبارسنجی فرمت)
- ورود با شماره موبایل و رمز عبور
- ارسال کد تایید پیامکی (موک - در کنسول نمایش داده می‌شود)
- توکن JWT با مدت اعتبار 30 روز

### 💳 مدیریت کارت‌ها
- افزودن کارت با شماره 16 رقمی
- اعتبارسنجی با الگوریتم Luhn
- شناسایی بانک از روی BIN (14 بانک ایرانی)
- نمایش کارت‌ها با ماسک امنیتی
- حذف کارت

### 💸 پرداخت‌ها
- ایجاد پرداخت به شماره کارت
- پردازش از طریق شتاب موک (تاخیر 2.5 ثانیه)
- نرخ موفقیت 90% (قابل تنظیم)
- کد پیگیری 12 رقمی
- اطلاع‌رسانی پیامکی به گیرنده

### 📝 درخواست پرداخت
- ایجاد درخواست با مبلغ و توضیحات
- کد اشتراک‌گذاری 8 کاراکتری
- مدت اعتبار 7 روز
- وضعیت‌ها: در انتظار، پرداخت شده، منقضی شده
- لینک قابل اشتراک‌گذاری

### 📊 تراکنش‌ها
- نمایش تاریخچه تراکنش‌ها
- فیلتر دریافتی/ارسالی
- نمایش تاریخ فارسی نسبی ("2 ساعت پیش")
- کد پیگیری برای هر تراکنش

</div>

---

## 🔧 API Endpoints

### Authentication
```
POST   /api/auth/register          # ثبت نام
POST   /api/auth/login             # ورود
POST   /api/auth/send-code         # ارسال کد تایید
POST   /api/auth/verify-code       # تایید کد
```

### User
```
GET    /api/user/profile           # پروفایل کاربر
GET    /api/user/cards             # لیست کارت‌ها
POST   /api/user/cards             # افزودن کارت
DELETE /api/user/cards/:id         # حذف کارت
PUT    /api/user/cards/:id/primary # تنظیم کارت اصلی
```

### Payments
```
GET    /api/payments/transactions       # تراکنش‌ها
POST   /api/payments/create             # ایجاد پرداخت
GET    /api/payments/requests           # درخواست‌های پرداخت
POST   /api/payments/requests           # ایجاد درخواست
GET    /api/payments/requests/:code     # جزئیات درخواست
POST   /api/payments/requests/pay       # پرداخت درخواست
```

### Demo/Mock
```
POST   /api/mock/reset             # بازنشانی داده‌های دمو
GET    /api/mock/demo-users        # لیست کاربران دمو
GET    /api/health                 # وضعیت سرور
```

---

## 🎨 تکنولوژی‌ها

### Backend
- **Node.js** - Runtime
- **Express** - Web Framework
- **JWT** - Authentication
- **bcryptjs** - Password Hashing
- **moment-jalaali** - Persian Dates
- **uuid** - ID Generation

### Mobile
- **React Native** - Framework
- **Expo** - Development Platform
- **React Navigation** - Navigation
- **AsyncStorage** - Local Storage
- **Axios** - HTTP Client

---

## 🧪 تست کردن

<div dir="rtl">

### 1. تست Backend با curl

```bash
# سلامتی سرور
curl http://localhost:3000/api/health

# ورود
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"phone":"09123456789","password":"demo1234"}'

# دریافت کاربران دمو
curl http://localhost:3000/api/mock/demo-users
```

### 2. تست Mobile App

1. اپلیکیشن را باز کنید
2. روی یکی از کاربران دمو کلیک کنید
3. "ورود" را بزنید
4. صفحه اصلی باز می‌شود با:
   - تراکنش‌های اخیر
   - درخواست‌های در انتظار
   - دکمه‌های اقدام سریع
5. "کارت‌های من" را امتحان کنید
6. "درخواست پرداخت" ایجاد کنید

</div>

---

## 📱 اسکرین‌شات‌ها

<div dir="rtl">

### صفحه ورود
- بنر دمو
- کاربران دمو برای ورود سریع
- فرم ورود
- لینک ثبت نام

### صفحه اصلی
- پیام خوش‌آمدگویی
- دکمه‌های اقدام سریع
- درخواست‌های در انتظار
- تراکنش‌های اخیر

### صفحه کارت‌ها
- لیست کارت‌ها با طراحی زیبا
- فرم افزودن کارت
- نشان "کارت اصلی"

### صفحه درخواست پرداخت
- ورودی مبلغ بزرگ
- فیلد توضیحات
- مثال‌های سریع
- اطلاعات اعتبار

</div>

---

## 🔒 امنیت در حالت دمو

<div dir="rtl">

**توجه: این نسخه دمو فقط برای نمایش است و نباید در محیط پروداکشن استفاده شود.**

- ✅ رمزها با bcrypt هش می‌شوند
- ✅ JWT برای احراز هویت
- ✅ اعتبارسنجی ورودی‌ها
- ⚠️ بدون HTTPS (برای دمو محلی)
- ⚠️ بدون Rate Limiting
- ⚠️ JWT Secret ساده (در .env تغییر دهید)

</div>

---

## 🎭 ویژگی‌های دمو

<div dir="rtl">

### Backend Logs
تمامی عملیات در کنسول نمایش داده می‌شوند:

```
📱 MOCK SMS SERVICE
📱 To: 09123456789
📱 Code: 12345
📱 Message: کد تایید شما: 12345

💳 MOCK: Processing Shetab payment
   From: 603799******1234
   To: 627961******5678
   Amount: 500,000 ریال
✅ MOCK: Payment successful - Tracking code: 123456789012
```

### حالت آفلاین
اپلیکیشن موبایل می‌تواند کاملا بدون backend کار کند:
- داده‌های دمو در AsyncStorage
- تمام قابلیت‌ها عملیاتی
- تاخیرهای واقع‌گرایانه

### بازنشانی دمو
```bash
# از طریق API
curl -X POST http://localhost:3000/api/mock/reset

# یا از کد:
await api.resetDemoData();
```

</div>

---

## 🚀 استقرار (برای دمو)

<div dir="rtl">

### Backend
```bash
# Heroku, Railway, Render, etc.
cd backend
npm install
npm start
```

### Mobile
```bash
# Build for web
cd mobile
npm run web

# یا Expo EAS Build برای iOS/Android
expo build:android
expo build:ios
```

</div>

---

## 🛠️ توسعه

<div dir="rtl">

### افزودن بانک جدید
در `backend/src/database/mockData.js`:
```javascript
const IRANIAN_BANKS = {
  '603799': { name: 'بانک ملی ایران', nameEn: 'Bank Melli Iran' },
  // بانک جدید اضافه کنید
  '123456': { name: 'بانک جدید', nameEn: 'New Bank' }
};
```

### تغییر نرخ موفقیت پرداخت
در `backend/.env`:
```
MOCK_PAYMENT_SUCCESS_RATE=0.9  # 90% success
```

### تغییر تاخیر پیامک
در `backend/.env`:
```
MOCK_SMS_DELAY=5000  # 5 seconds
```

</div>

---

## 📝 TODO (برای نسخه پروداکشن)

<div dir="rtl">

- [ ] اتصال به PostgreSQL واقعی
- [ ] اتصال به API واقعی شتاب
- [ ] اتصال به سرویس پیامک (کاوه‌نگار/قاصدک)
- [ ] افزودن Redis برای Cache
- [ ] پیاده‌سازی Rate Limiting
- [ ] افزودن HTTPS
- [ ] پیاده‌سازی 2FA
- [ ] افزودن تست‌های واحد
- [ ] افزودن CI/CD
- [ ] افزودن Monitoring و Logging

</div>

---

## 🤝 مشارکت

<div dir="rtl">

این پروژه یک نسخه دمو/نمایشی است. برای پروداکشن، لطفا:
1. تمام سرویس‌های موک را با API واقعی جایگزین کنید
2. امنیت را تقویت کنید
3. از پایگاه داده واقعی استفاده کنید
4. تست‌های کامل بنویسید

</div>

---

## 📄 مجوز

MIT License - این پروژه برای نمایش و آموزش است.

---

## 💡 پشتیبانی

<div dir="rtl">

برای سوالات یا مشکلات:
- Issue در GitHub باز کنید
- مستندات API را بررسی کنید
- لاگ‌های Backend و Mobile را چک کنید

</div>

---

<div dir="rtl" align="center">

### ساخته شده با ❤️ برای نمایش به سرمایه‌گذاران و ذینفعان

**نسخه دمو - بدون نیاز به تنظیمات پیچیده**

🎭 حالت دمو فعال | 🚀 آماده برای نمایش | ✅ تمام ویژگی‌ها عملیاتی

</div>

